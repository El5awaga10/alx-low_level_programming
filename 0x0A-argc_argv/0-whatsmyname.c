#include <stdio.h>

/**
 * main- Entry point
 * @argc: a counter
 * @argv: an array
 * Return: return 0
 */

int main (int argc, char *argv[])
{
	printf ("the name of the program is %s", argv[0]);
	printf ('\n');
	return (0);
}
