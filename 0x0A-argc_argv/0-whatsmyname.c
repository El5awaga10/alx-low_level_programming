#include <stdio.h>

/**
 * main- Entry point
 * @argc: a counter
 * @argv: an array
 * Return: return 0
 */

int main (int argc __attribute__((unused)), char *argv[])
{
	printf ("the name of the program is %s", argv[0]);
	return (0);
}
