#include "main.h"

/**
 * _abs- absolute value of an integer.
 * Return: Always 0
 */

int _abs(int)
{
	int i;
	if (i < 0)
		_putchar (-i);
	else if (i > 0)
		_putchar (i);
	else (i = 0)
		_putchar ('0');
	return (0);
}
