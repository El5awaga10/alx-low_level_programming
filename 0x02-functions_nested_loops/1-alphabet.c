#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
 * main- entry point
 * prints the alphabet
 * Return: Always 0
 */

int main(void)
{
	int i= 0;
	if (islower(c))
		while (i!=EOF)
		{
                        char c;
			_putchar(c);
			i++;
		}
	_putchar('\n');
        return (0);
}
